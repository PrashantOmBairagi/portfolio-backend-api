spring.application.name=contactApi

# ── Database (disabled — cloud PostgreSQL instance expired) ──────────────────
# Restore these env vars on Render and remove the @SpringBootApplication exclude
# when a new DB is provisioned.
# spring.datasource.url=${MDB_URL}
# spring.datasource.username=${MDB_USERNAME}
# spring.datasource.password=${MDB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.datasource.hikari.maximum-pool-size=2

# ── Spring Mail (Gmail SMTP) ──────────────────────────────────────────────────
# Set MAIL_USERNAME and MAIL_PASSWORD as environment variables on Render.
# MAIL_PASSWORD must be a Gmail App Password (not your regular password).
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

server.port=${PORT:8080}